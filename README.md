# AI-application-for-education
This is my graduation design of application which uses AI models in education fields.

## MVP commands
- Backend API (FastAPI): `uvicorn backend.app.main:app --reload`
- Frontend dev server (Vue/Vite): `npm install` then `npm run dev` (run inside `frontend/`)
